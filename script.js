let currentSection=0;const sections=document.querySelectorAll('.section');const totalSections=sections.length;const navLinks=document.querySelectorAll('.header nav ul li a');const headerHeight=document.querySelector('.header').offsetHeight;const fullpageContainer=document.querySelector('.fullpage-container');function scrollToSection(index){if(index<0||index>=totalSections)return;currentSection=index;const targetSection=sections[currentSection];window.scrollTo({top:targetSection.offsetTop,behavior:'smooth'})}
window.addEventListener('wheel',(event)=>{if(event.deltaY>0){scrollToSection(currentSection+1)}else{scrollToSection(currentSection-1)}});let touchStart=0;window.addEventListener('touchstart',(event)=>{touchStart=event.touches[0].clientY});window.addEventListener('touchend',(event)=>{const touchEnd=event.changedTouches[0].clientY;if(touchEnd<touchStart){scrollToSection(currentSection+1)}else if(touchEnd>touchStart){scrollToSection(currentSection-1)}});navLinks.forEach((link,index)=>{link.addEventListener('click',(event)=>{event.preventDefault();const targetSection=sections[index];fullpageContainer.scrollTo({top:targetSection.offsetTop,behavior:'smooth'})})})